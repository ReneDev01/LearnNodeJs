<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/asset/semantic.min.css">
    <style>
        .main.container
        {
            margin-top: 7rem;
        }
    </style>
</head>
<body>

    <div class="ui fixed inverted menu">
        <a href="/" class="header item">Accueil</a>
    </div>

    <div class="ui main container">
        
        <% if(locals.flash && locals.flash.error){ %> 
            <div class="ui negative message">
                <%= flash.error %> 
            </div>
        <% } %> 

        <% if(locals.flash && locals.flash.success ){ %>
            <div class="ui positive message">
                <%= flash.success %> 
            </div>
        <% } %> 

        <form action="/" method="post" class="ui form">
            <div class="field">
                <label for="publication">Publication</label>
                <textarea name="publication" id="publication" cols="30" rows="10"></textarea>
            </div>

            <button type="submit" class="ui blue labeled submit icon button">
                <i class="icon edit"></i> Envoyer
            </button>
        </form>

        <div class="ui comments">
            <h3 class="ui dividing header">
                Commantaires
            </h3>
            <% for(publication of publications) {%> 
                <div class="comment">

                    <a href="/publication/<%= publication.id %>" class="avatar">
                        <img src="/asset/image/prs.png" alt="" srcset="">
                    </a>                 
                    <div class="content">
                        <div class="metadata">
                            <div class="date">
                                <%= publication.created_at.fromNow() %> 
                            </div>
                        </div>
                        <div class="text">
                            <%= publication.content %> 
                        </div>
                    </div>
                </div>
            <% } %> 
        </div>
       
    </div>

</body>
</html>